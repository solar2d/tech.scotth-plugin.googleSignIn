Sources for the plugin `plugin.googleSignIn`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.googleSignIn" = {
            publisherId = "tech.scotth",
        },
    },
}
```
